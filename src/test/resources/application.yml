spring:
  datasource:
    url: jdbc:mysql://localhost:3306/studay_test?serverTimezone=Asia/Seoul
    username: root
    password: 7351
    driver-class-name:

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
    open-in-view: false

logging:
  level:
    org:
      hibernate:
        type:
          descriptor:
            sql: debug

geocode:
  clientId: ujxd24s42f
  clientSecret: 9y48glIQWs8BHmeGttck5gcYS684JWjqxzmceIwx
  clientIdProperty: X-NCP-APIGW-API-KEY-ID
  clientSecretProperty: X-NCP-APIGW-API-KEY
  apiUrl: https://naveropenapi.apigw.ntruss.com/map-geocode/v2/geocode
springdoc:
  default-consumes-media-type: application/yml;charset=UTF-8
  default-produces-media-type: application/yml;charset=UTF-8

